node_modules
dist
.astro
uploads
data.db
.env
.wrangler
.stream.env
.venv
.vscode
.DS_Store
docs/company/

# Python bytecode from local scripts
__pycache__/

# Lighthouse CI raw reports: 25 MB per run locally, and since #399 CI does not
# keep its own either — uploadArtifacts is off there, because 219 uploads of
# them exhausted the repository's artifact quota. The record that survives a
# run is the summary committed under docs/audits/ (#305).
.lighthouseci/

# Regenerated by docs/design/hero-rotativo/generate.py as a hand-off to the
# design canvas: the six SVGs it inlines are already tracked under public/.
docs/design/hero-rotativo/images.json

# The untouched NASA originals grounds.py downloads before treating them. The
# TREATED files under public/assets/hero/ground/ are tracked and are what the
# site serves; these are 0.2-2 MB each and re-fetchable by nasa_id.
docs/design/hero-rotativo/.cache/
