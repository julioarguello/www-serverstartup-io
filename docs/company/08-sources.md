# Source registry

The detailed source registry — Dropbox document paths, Google Drive fileIds, Gmail
thread references and recovery commands — is maintained **privately** in
`private/08-sources.md` (gitignored; available locally to the agent, never
committed).

Public files in this knowledge base cite public evidence inline (GitHub
repositories, sector press). Every private fact keeps its full citation in the
private registry.

Known gaps and pending verifications are tracked there as well.
